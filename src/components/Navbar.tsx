const Navbar = () => {
  return (
    <nav className="flex bg-state justify-center items-center p-8 bg-slate-900 text-slate-300 text-2xl">
        <a href="#" className="bg-black p-3 rounded-lg hover:bg-white hover:text-black transition-all" >
          Paleta de colores
        </a>
    </nav>
  );
};

export default Navbar;
